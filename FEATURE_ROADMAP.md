# GitHub Batch Manager 功能方案

## 背景

基于当前全量代码评估，这个项目已经具备比较完整的本地多仓库批量运维基础能力：

- 批量扫描、导入、保存本地 Git 仓库
- 仓库状态总览
- 批量刷新、拉取、提交、推送
- 日志树、提交详情、文件变更查看
- 列布局持久化
- Windows 资源管理器右键菜单联动

当前更适合把产品从“批量操作器”继续演进成“多仓库工作台”。

---

## 目标方向

后续功能建议优先围绕以下 4 个方向展开：

1. 仓库组织能力
2. Git 安全操作能力
3. 分支与远端管理能力
4. 自动化与报告能力

---

## 功能方案清单

### 1. 仓库搜索 / 分组 / 工作区视图

**目标**：
让几十个仓库的管理不再依赖纯手动滚动查找。

**建议功能**：

- 仓库名称 / 路径 / 分支关键字搜索
- 标签系统，例如：`工作中`、`归档`、`实验性`
- 收藏仓库
- 自定义分组 / 工作区视图
- 保存筛选条件

**价值**：

- 仓库数量变多后，查找效率会明显提升
- 为后续“批量对某个工作区执行操作”打基础

---

### 2. 远端健康检查与异常面板

**目标**：
快速发现并定位远端配置类问题。

**建议功能**：

- 检测 `origin` 是否存在
- 检测远端 URL 是否可访问
- 检测仓库不存在 / 权限失败 / 认证失败
- 检测 detached HEAD
- 检测未配置 upstream
- 检测 ahead / behind 异常
- 提供“仅看异常仓库”视图

**价值**：

- 你当前真实使用场景里已经频繁出现这类错误
- 非常适合批量仓库运维工具

---

### 3. 批量分支管理

**目标**：
支持多仓库协同开发时的统一分支操作。

**建议功能**：

- 批量创建分支
- 批量切换分支
- 批量检查某分支是否存在
- 批量设置 upstream
- 批量切回 `main` / `master`
- 显示各仓库当前分支一致性

**价值**：

- 多仓库联动开发时非常高频
- 与当前架构兼容性好，易于增量实现

---

### 4. 安全拉取 / 安全提交预检

**目标**：
降低批量操作误伤风险。

**建议功能**：

- 拉取前先 `fetch` 再做差异判断
- 标记脏工作区仓库并支持自动跳过
- 无 upstream 仓库自动归类为待处理
- pull 前预览风险摘要
- 冲突风险预警
- 可选的自动 stash / 恢复
- commit 前显示待提交文件数量与摘要

**价值**：

- 当前 `commit` 仍偏“全量提交”风格
- 当前 `pull` 缺少更细的保护逻辑

---

### 5. 工作区变更 / Diff 面板

**目标**：
从“只能批量执行”升级到“可精细确认再执行”。

**建议功能**：

- 查看当前工作区未提交文件
- 查看文件 Diff 摘要
- 选择性暂存
- 选择性提交
- 查看未推送提交列表

**价值**：

- 会显著提升单仓库处理体验
- 让这个工具不只是运维面板，也能承担轻量日常 Git 客户端角色

---

### 6. 任务预设 / 一键批处理

**目标**：
把重复流程固化成可复用动作。

**建议功能**：

- 保存一组仓库选择
- 保存一组批量动作组合
- 预设示例：
  - 每日刷新
  - 工作区批量拉取
  - 仅检测异常远端
  - 批量推送已领先仓库

**价值**：

- 减少重复点选
- 更像真正的桌面工具，而不是一次性脚本面板

---

### 7. 执行报告导出

**目标**：
让批量操作结果更容易归档、复盘、分享。

**建议功能**：

- 导出 CSV
- 导出 Markdown
- 导出 HTML
- 报告内容包含：
  - 仓库名
  - 路径
  - 分支
  - 动作结果
  - 错误原因
  - 时间

**价值**：

- 适合团队协作和问题追踪
- 与当前日志体系天然契合

---

### 8. GitHub / `gh` CLI 联动

**目标**：
把本地仓库面板和 GitHub 协作动作接起来。

**建议功能**：

- 打开仓库主页
- 打开 Issues / PR 页面
- 检查当前仓库是否存在未合并 PR
- 读取 `gh` CLI 基本信息
- 查看默认分支 / 可用远端信息

**价值**：

- 不需要立刻引入 GitHub API
- 用轻量联动就能增加很多实际价值

---

### 9. 生命周期提醒

**目标**：
帮助长期维护大量仓库时做清理与归档。

**建议功能**：

- 长期未更新仓库提醒
- 本地路径失效提醒
- 远端已删除提醒
- 长期未推送提交提醒
- 长期落后仓库提醒

**价值**：

- 适合你这种一屏管理大量仓库的场景

---

## 优先级建议

### P0：最值得先做

1. 仓库搜索 / 分组 / 工作区视图
2. 远端健康检查与异常面板
3. 批量分支管理

### P1：体验和安全性提升

4. 安全拉取 / 安全提交预检
5. 工作区变更 / Diff 面板
6. 任务预设 / 一键批处理

### P2：生态联动与运营型能力

7. 执行报告导出
8. GitHub / `gh` CLI 联动
9. 生命周期提醒

---

## 推荐开发顺序

### 第一阶段

- 仓库搜索 / 分组 / 工作区视图
- 远端健康检查与异常面板
- 批量分支管理

### 第二阶段

- 安全拉取 / 安全提交预检
- 工作区变更 / Diff 面板

### 第三阶段

- 任务预设 / 一键批处理
- 执行报告导出
- GitHub / `gh` CLI 联动
- 生命周期提醒

---

## 结论

如果只优先做 3 项，建议先做：

1. 仓库搜索 / 分组 / 工作区视图
2. 远端健康检查与异常面板
3. 批量分支管理

这三项最贴合当前产品定位，也最容易基于现有架构自然扩展。
