# GitHub Batch Manager

基于 Rust + Tauri + Vue3 的桌面应用，用于管理本地 Git 仓库。

## 目录

- `frontend/`：前端界面
- `src-tauri/`：Rust 后端

## 运行

```powershell
cd src-tauri
cargo tauri dev
```

## 构建

```powershell
cd src-tauri
cargo build
```
