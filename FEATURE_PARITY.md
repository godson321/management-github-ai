# Python 旧版功能对齐表

| 功能 | Python 旧版 | 当前 Tauri + Vue3 版 | 状态 |
|---|---|---|---|
| 桌面壳 | Tkinter 窗口 | Tauri v2 桌面窗口 | 已对齐 |
| 仓库导入 | 目录选择器、手动多行路径导入 | 多行路径导入，后端递归发现 Git 仓库 | 部分对齐：目录选择器待补 |
| 仓库持久化 | 本地 JSON 保存路径和勾选状态 | Tauri 本地 JSON 保存路径和勾选状态 | 已对齐 |
| 启动刷新 | 启动后刷新保存仓库状态 | 当前保留手动刷新 | 未对齐 |
| 仓库表格 | 选择、仓库、分支、改动、领先、落后、最新提交、状态、路径 | Element Plus 表格显示相同核心数据，仓库列内展示路径 | 已对齐 |
| 表格排序 | 点击列头排序 | Element Plus 列排序 | 已对齐 |
| 表格列设置 | 右键列头隐藏、显示、移动、自动列宽、重置 | 当前未实现列配置菜单 | 未对齐 |
| 勾选操作 | 全选、清空、行勾选 | 全选、清空、行勾选 | 已对齐 |
| 失败项筛选 | 只看失败项 | 只看失败项 | 已对齐 |
| 搜索 | 搜索名称、分支、路径、状态 | 搜索名称、分支、路径、状态 | 已对齐 |
| 批量刷新 | 刷新选中仓库状态 | 刷新选中仓库状态 | 已对齐 |
| 批量拉取 | 支持合并、仅快进、变基策略，逐项输出日志 | 支持合并、仅快进、变基策略，新增 `batch-progress` 逐项日志 | 已对齐 |
| 批量提交 | 输入提交说明，`git add -A` 后提交 | Element Plus 弹窗输入提交说明，后端沿用 `git add -A` | 已对齐 |
| 批量推送 | 推送选中仓库 | 推送选中仓库 | 已对齐 |
| 运行中状态 | 单仓库运行中行状态和底部日志 | `batch-progress` 更新运行中状态和底部日志 | 已对齐 |
| 仓库详情 | 名称、路径、分支、同步、状态、错误 | 名称、路径、分支、同步、状态、错误 | 已对齐 |
| 日志树 | 提交图、HEAD、远端、标签、作者、日期 | 提交列表显示图标、HEAD/REMOTE/TAG 标记、作者、日期 | 已对齐 |
| 提交详情 | `git show --stat` 文本和文件变更表 | 提交详情文本和文件变更表 | 已对齐 |
| 提交详情缓存 | Python 侧缓存已加载提交详情 | 当前前端只保留当前仓库记录内数据 | 部分对齐 |
| 右键仓库菜单 | 刷新、拉取、提交、推送、打开目录、复制路径、Windows Explorer 菜单 | 当前未实现右键菜单 | 未对齐 |
| Windows Explorer 菜单 | PowerShell COM 读取并执行资源管理器右键菜单 | 当前 Tauri 版未迁移 | 未对齐 |
| UI 状态持久化 | 窗口、分栏、列布局、失败筛选、拉取策略 | 当前仅仓库列表持久化 | 未对齐 |

## 本轮重构重点

- 保留 Tauri/Rust 作为桌面壳和本机 Git 能力层。
- 前端重构为 Vue3 + Element Plus + TypeScript。
- 批量操作补齐逐项进度事件，修复“批量拉取等按钮执行时下方日志没有输出”的体验问题。
